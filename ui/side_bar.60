
import { ScrollView } from "sixtyfps_widgets.60";	
import { SearchBox } from "search_box.60";
import { ListenSection } from "listen_section.60";

SideBar := Rectangle {
	width: 185px;
	preferred-height: 200px;

	ScrollView {
		VerticalLayout {
		spacing: 10px;

			SearchBox {
				// This needs to be enabled to get the search box to the correct scale
				// width: parent.width - 20px;
			}
			ListenSection {  }
		}
	}
}
